 STEP 1: Upload rice_dataset.zip manually when prompted
from google.colab import files
uploaded = files.upload()  # 👉 Upload rice_dataset.zip here

# STEP 2: Extract the zip file
import zipfile
with zipfile.ZipFile("/content/rice_dataset.zip", 'r') as zip_ref:
    zip_ref.extractall("/content/")
print("✅ Dataset Extracted Successfully!")
